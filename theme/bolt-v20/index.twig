{% extends "master.twig" %}

{% block bodyclass %}body-home{% endblock %}

           
{% block content %}

<section class="hero">
	<div class="maincolumn">

		<article class="row">
            <h1>{{ record.title}}</h1>
            <h2 class="subheader articlebody">{{ record.subtitle }}</h2>
        </article>
    
    </div>
</section>

<section class="shorttexts">
	<article>
		<i class="fa fa-cogs fa-2x"></i>
		<h3>Open source</h3>
	</article>
	<article>
		<i class="fa fa-mobile fa-2x"></i>
		<h3>Responsive</h3>
	</article>
	<article>
		<i class="fa fa-group fa-2x"></i>
		<h3>Community-driven</h3>
	</article>
	<article>
		<i class="fa fa-magic fa-2x"></i>
		<h3>Easy to use</h3>
	</article>
</section>

<section class="maininfo">
    
    {% setcontent devblock = 'blocks/developers' %}
    {% setcontent frontendblock = 'blocks/frontend-designers' %}
    {% setcontent editorblock = 'blocks/content-editors' %}
    
    {% set primaryblocks = [devblock,frontendblock,editorblock] %}

    {% for item in primaryblocks %}
    <article class="column">
        <h2>{{ item.title }}</h2>
        <h3 class="subheader">{{ item.subtitle }}</h3>
        {{ item.body }}
        <!-- a href="{{item.sitelink}}" class="button">{{ item.calltoaction }}</a -->          
    </article>
    {% endfor %}
    
</section>
    
<section class="features">
   <div class="maincolumn">
       <h1>Features</h1> 
       {% setcontent features = 'features' where {'frontpage':'0'} %}
       <ul class="featurelist">
            {% for item in features %}
            <li>
                <h3><i class="fa fa-bolt"></i>{{ item.title }} -{{item.frontpage}}-
            </h3>
                {{ item.text }} 
            </li>
            {% endfor %}
        </ul>
        <a href="/pages/features" class="button">See more features</a>
    </div>
</section>

<section class="slider">
    <div class="inner">
    {% setcontent slideshow = 'slides' %}
    <div class='pager'></div> {# automatically filled #}
    <div class="cycle-slideshow"
        data-cycle-swipe='true'
        data-cycle-fx='scrollHorz'
        data-cycle-timeout='0'
        data-cycle-pager=".pager"
        data-cycle-slides="> div"
        >
        {% for item in slideshow %}
        <div class="slide" data-cycle-pager-template="<a href=#>{{item.title}}</a> ">
            <div class="slideimg">
                <img class="large" src="{{ thumbnail(item.image, 700, 530,"c") }}">
                <img class="mobile" src="{{ thumbnail(item.imagemobile, 250, 435,"c") }}">
            </div>
            <div class="highlights">
            {{item.body}}
            </div>
        </div>
        {% endfor %}
    </div>
    </div>
</section>

<section class="secinfo">
    {% setcontent supportblock = 'blocks/support' %}
    {% setcontent docblock = 'blocks/documentation' %}
    {% setcontent contributeblock = 'blocks/contribute' %}
    
    {% set secblocks = [supportblock,docblock,contributeblock] %}

    {% for item in secblocks %}
    <article class="column">
        <h2>{{item.title}}</h2>
        <h3 class="subheader">{{item.subtitle}}</h3>
        {{item.body}}
        <a href="{{item.sitelink}}" class="button">{{item.calltoaction}}</a>          
    </article>
    {% endfor %}
</section>

<section class="shorttexts">
	<article>
		<div class="icon"><a href="http://symfony.com/"><img src="{{paths.theme}}images/symfony_logo.png" width="100" alt="Symfony logo" /></a></div>
		<h3>Built on Silex/Symfony</h3>
	</article>
	<article>
		<div class="icon"><a href="http://twig.sensiolabs.org/"><img src="{{paths.theme}}images/twig_logo.png" width="100" alt="Twig logo" /></a></div>
		<h3>Twig template engine</h3>
	</article>
	<article>
		<div class="icon"><a href="http://github.com"><img src="{{paths.theme}}images/octocat.png" width="100" alt="Github" /></a></div>
		<h3>Github</h3>
	</article>
	<article>
		<div class="icon"><a href="#"><img src="{{paths.theme}}images/octocat.png" width="100" alt="Github" alt="Here be logo" /></a></div>
		<h3>Debug bar</h3>
	</article>
</section>

{% endblock content %}

{% block footer_extra_scripts %}
    <script src="{{paths.theme}}scripts/picturefill.js"></script>
    <script src="{{ paths.theme }}scripts/cycle2.min.js"></script>
    <script src="{{ paths.theme }}scripts/cycle2.swipe.min.js"></script>
{% endblock %}